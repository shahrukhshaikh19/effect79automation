# VELAR producer run — skill / workflow defects

Session context: `producer-velar-unbox`  
Started: 2026-09-06 (third init after lock fix + project wipe)  
Rule: note defects as they appear. Do not hide them. This is not a beauty scorecard.

Quality bar: stranger names a compact hinged case + two in-ear instruments from clay. Soft-matte off-white is CMF after form. Website only after Product Form Gate APPROVED. Four Gemini stills are mood / product-read — not pixel reconstruction, not an AirPods clone brief.

## Prior (still true)

- W5 — producer `pass` banned. Keep.
- W8 — MCP stamp vs live ping. Confirm after ping.
- W9 — Windows init mojibake if prompt is not UTF-8.
- S1 — ACOS-16 recipe text did not create a product. This run uses `tools/form/build_form.py` + `clay_look.yaml`.

## This run — `host-ec0ff110e917`

### W13 — Mood stills not copied onto disk

User attached four Gemini JPGs. Host copy from the Cursor assets path returned empty. ACOS-02 was written from the chat stills. Compare loop has no files under `evidence/mood-refs/`.

### W14 — Design Gate + invoke shrink worked

CREATIVE invoke_now was ACOS-01 + ACOS-02 only. ACOS-03/04/05 stayed locked. Gate APPROVED without the quintet.

### S3 — Checked-in `build_form.py` still dumps two eggs

Iteration 1 clay (`evidence/form-clay/`): two faceted ellipsoids. No wells. Buds not visible. Hinge not readable. `proportion` framed one disc. Names `Case_*` / `Earbud_*` exist; product-read fails.

This is the same disease as `_clay_iterN.py`, just a stable filename. Look lock did its job (`product_read_verdict: fail`). Next hop must change the builder (one mass → split → wells → three-volume buds in the wells), not write a novel.

### S4 — Iteration 2: split mass, still no product

One pebble bisected into lid/base. Clay now reads as two empty bowls with a gap. Wells did not cut (boolean applied after subdiv). Lid exploded (W10 again: parent without origin + inverse). Buds still not in the picture. `product_read_verdict` stays `fail`. Website not started.

### S6 — Iteration 4–5: class read on some views only

`front34` / `rear` now read as a compact case plus two seated buds. Front still a low bar. Profile is a lumpy side. Buds are domes, not housing+tip+stem. This is the first time a stranger *could* say “case + two buds” from one still. It is not production grade. Stay `fail`. No website.

### S5 — Iteration 3: buds appear, still kitbash

`front34` now shows two spheres in a dish. That is the first instrument read. Front/profile still fail. Boolean wells tore the base into a ring. Lid/hinge still not a designed joint. Verdict stays `fail`. No `ready_for_critic`. No website.

### Skill note

ACOS-15 SKILL.md still lists “wide bar / oversized package” as a skill reject. Host regex for that was removed. Skill text and host disagree.
