# Codex Adapter — Context Strategy

Minimum bootstrap → canonical refs → task class → skill metadata → activated SKILL.md → on-demand assets.

Codex CLI sessions should externalize progress to `docs/PROGRESS_LEDGER.md` pattern — not invent chat-only memory as ACOS memory.

Model limits: use `templates/MODEL_PROFILE_TEMPLATE.md` when profiles exist; do not invent benchmark scores.
