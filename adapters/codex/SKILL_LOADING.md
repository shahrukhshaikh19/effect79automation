# Codex Adapter — Progressive Skill Loading

Registry-driven loading — `registry/SKILLS.yaml`.

L1 → L2 → L3 per shared contract in `registry/ADAPTERS.yaml`.

Do not duplicate skill lists or SKILL.md bodies in adapter files.

EXT-FE-01/02: reference only; do not copy — licensing restricted.
